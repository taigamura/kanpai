// Web stub for the UI Studio (expo start --web). AdMob banners are native-only; on web this
// renders nothing so the app bundles and runs in the browser. Native builds use BannerAdSlot.tsx.
export function BannerAdSlot() {
  return null;
}
